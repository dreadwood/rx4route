mixin f-field-select({ placeholder, options })
  .f-field-select
    .f-field-select__icon
      +svg({ name: "icon-arrow-select", width: 28, height: 23 })
    select(placeholder=placeholder)
      each it in options
        option(value=it)=it
