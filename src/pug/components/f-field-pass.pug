include ../elements/images

mixin f-field-pass({})
  fieldset.f-field-pass
    input.f-field(type="password" placeholder="Password")
    input.f-field(type="password" placeholder="Confirm")
    button.f-field-pass__visible(type="button" onclick="this.classList.toggle('show'); this.blur();" aria-label="show password")
      +svg({ name: "icon-pass-hide", width: 32, height: 32 })
      +svg({ name: "icon-pass-show", width: 32, height: 32 })
